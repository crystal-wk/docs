# Source: https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83

### Uh oh!

There was an error while loading. [Please reload this page]().

[bytedance](https://github.com/bytedance) / **[deer-flow](https://github.com/bytedance/deer-flow)** Public

- [Notifications](https://github.com/login?return_to=%2Fbytedance%2Fdeer-flow) You must be signed in to change notification settings
- [Fork 10.9k](https://github.com/login?return_to=%2Fbytedance%2Fdeer-flow)
- [Star 79.8k](https://github.com/login?return_to=%2Fbytedance%2Fdeer-flow)

 ## File tree

Expand file treeCollapse file tree

TopOpen diff view settings

Filter options

- backend

 - [AGENTS.md](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-2c0330701aa3b374d2869cd99dc511b7220af9fb606a7981982d8ce5bc247554)

 - packages/harness/deerflow

 - agents

 - middlewares

 - [\_bounded\_dict.py](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-1daba0335079a7fc8a9ed2b9f783b7bcd68c650180a3d029bdd1b2fd753fae3c)

 - [delegation\_ledger.py](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-17f902606bf223d58833acf5f0c0db59ae27a7d6af37971ce8e86be3f53ded51)

 - [loop\_detection\_middleware.py](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-5dc6e809115603e75eaa4512225adac1a16412200146afd8d173fe739532421f)

 - [token\_budget\_middleware.py](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-ddd9c7dccd3e757c3c60c929673525014ca3fc75f351aa4a269864d6749fa0fe)

 - [tool\_error\_handling\_middleware.py](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-90dc26636af88ed6b25dd098503a434e3f212ed186c0b4a290824c109ed5e18e)

 - [thread\_state.py](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-c910b342ea4733266d2037cd1d21e0ca58597d191125e6ab2534047f901892e3)

 - config

 - [subagents\_config.py](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-e07e4538d4b91882adb32ad871a6235ee1b4bd4b67066021998daee26d2f9a7b)

 - subagents

 - [executor.py](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-f31fac0a4ed6e0ac181f32198733f67f943f42e09f345e8bb626e2c10b5bebd4)

 - [status\_contract.py](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-1559066b73dd57ab82e6bc6e6d7701833475db1c582a451e878af449a9fcb3e2)

 - tools/builtins

 - [task\_tool.py](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-c0cd7b577bb356da032e688593b93d97c6c0d2afb786b1a125c5e99a15513def)

 - tests

 - [test\_delegation\_ledger.py](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-e9ddfb2d84ec6c441890665c951d2a6382a4548ee3c189e5b7e87f00afce7112)

 - [test\_loop\_detection\_middleware.py](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-8f9dad11d67f19e989a0e65526d015427896185cfaa6aabf1c3c834a80c81aa1)

 - [test\_subagent\_executor.py](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-a50119b17b47c5ab62710159269e4af833a7563d1396b0c6e2d04afe654f90a3)

 - [test\_subagent\_status\_contract.py](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-7e83778ad6501ded22fe124ee98c4ab47fff8b30c95547977a8c23ca76fb08e7)

 - [test\_task\_tool\_core\_logic.py](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-9c252474e0f97d21615003794b834b6d035745f0e47435344ea0fa705d679663)

 - [test\_token\_budget\_middleware.py](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-f87eca50670a371ad2af063e2cf12c7c775b8fb28c1ca1e5deda5b6e38a325b4)

 - [test\_tool\_error\_handling\_middleware.py](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-c4938f6563abd38599533f693e7f2ca989d5261aab7a643e1bd58468fe817002)

- [config.example.yaml](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-c7f43f17867c7240c4a26f92d204f0999bba302964d8f9abbc10e9103ffb066a)

- contracts

 - [subagent\_status\_contract.json](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-1098cd36ad0b8e7baeb55a57606913fd6b694646e088fcd44f8c1572d1bfe89d)

- frontend

 - src/core/tasks

 - [subtask-result.ts](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-0280ce7b116fc7f13e0ab1fe1e1a589b1a78c7d8a8f3fbe9bc7124d4ae5aa686)

 - [types.ts](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-0a1ae28cbb2935bf4798bb070138922827aadbde76c1f3e2faa6212036f137ca)

 - tests/unit/core/tasks

 - [subtask-result.test.ts](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-292f8cf39535a3487a98faf7d39f60d9922718842f09900c644ff198aede2c9e)

Expand file treeCollapse file tree

Top

Open diff view settings

Collapse file

### [`‎backend/AGENTS.md‎`](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-2c0330701aa3b374d2869cd99dc511b7220af9fb606a7981982d8ce5bc247554)

Copy file name to clipboardExpand all lines: backend/AGENTS.md

+2\-2Lines changed: 2 additions & 2 deletions

- Display the source diff
- Display the rich diff

| Original file line number | Diff line number | Diff line change |
| --- | --- | --- |
| 
`   @@ -245,7 +245,7 @@ Lead-agent middlewares are assembled in strict order across three functions: the   `

 |
| `245` | `245` | ``   22. **DeferredToolFilterMiddleware** - *(optional, if `tool_search.enabled`)* Hides deferred (MCP) tool schemas from the bound model until `tool_search` promotes them (reads per-thread promotions from `ThreadState.promoted`, hash-scoped)   `` |
| `246` | `246` | ``   23. **SystemMessageCoalescingMiddleware** - Merges every SystemMessage into a single leading SystemMessage per request; provider-agnostic fix for strict backends (vLLM/SGLang/Qwen/Anthropic) that reject non-leading system messages. Touches the per-request payload only (checkpoint state unchanged); on midnight crossings only the latest `dynamic_context_reminder` SystemMessage survives   `` |
| `247` | `247` | ``   24. **SubagentLimitMiddleware** - *(optional, if `subagent_enabled`)* Truncates excess `task` tool calls to enforce the `MAX_CONCURRENT_SUBAGENTS` limit   `` |
| `248` | | ``-  25. **LoopDetectionMiddleware** - *(optional, if `loop_detection.enabled`)* Detects repeated tool-call loops; hard-stop clears both structured `tool_calls` and raw provider tool-call metadata before forcing a final text answer  `` |
| | `248` | ``+  25. **LoopDetectionMiddleware** - *(optional, if `loop_detection.enabled`)* Detects repeated tool-call loops; hard-stop clears both structured `tool_calls` and raw provider tool-call metadata before forcing a final text answer; stamps `loop_capped` via `consume_stop_reason` (#3875 Phase 2), symmetric to `TokenBudgetMiddleware`  `` |
| `249` | `249` | ``   26. **TokenBudgetMiddleware** - *(optional, if `token_budget.enabled`)* Enforces per-run token limits   `` |
| `250` | `250` | ``   27. **Custom middlewares** - *(optional)* Any `custom_middlewares` passed to `build_middlewares` are injected here, before the safety/clarification tail   `` |
| `251` | `251` | ``   28. **SafetyFinishReasonMiddleware** - *(optional, if `safety_finish_reason.enabled`)* Suppresses tool execution when the provider safety-terminated the response (e.g. `finish_reason=content_filter`); registered after custom middlewares so LangChain's reverse-order `after_model` dispatch runs it first   `` |
| 

``   @@ -377,7 +377,7 @@ Proxied through nginx: `/api/langgraph/*` → Gateway LangGraph-compatible runti   ``

 |
| `377` | `377` | ``   **Concurrency**: `MAX_CONCURRENT_SUBAGENTS = 3` enforced by `SubagentLimitMiddleware` (truncates excess tool calls in `after_model`); default subagent timeout `subagents.timeout_seconds=1800` (30 min) and built-in `general-purpose` `max_turns=150` (raised from 100/15-min so deep-research subtasks stop hitting `GraphRecursionError` out of the box)   `` |
| `378` | `378` | ``   **Flow**: `task()` tool → `SubagentExecutor` → background thread → poll 5s → SSE events → result   `` |
| `379` | `379` | ``   **Events**: `task_started`, `task_running`, `task_completed`/`task_failed`/`task_timed_out`   `` |
| `380` | | ``-  **Turn-budget cap (#3875 Phase 2)**: `recursion_limit` on the subagent `run_config` equals `max_turns`, so exhausting the turn budget raises `GraphRecursionError` from `agent.astream`. `executor.py::_aexecute` catches it specifically (before the generic `except Exception`) and sets `SubagentStatus.MAX_TURNS_REACHED` — distinct from `FAILED` — with the **partial result recovered from the last streamed chunk** via `_extract_final_result` (which delegates to the shared `utils/messages.py::message_content_to_text`, returning a `"No response generated"` sentinel when no text survived). Previously the exception fell through to the generic handler and was misclassified as `FAILED`, so the lead could not tell "broken subagent" from "out of budget" and the work already streamed into `final_state` was discarded. `task_tool.py` returns it through the shared `_task_result_command(status="max_turns_reached", result=partial, error=cap)`, which `format_subagent_result_message` renders as `Task reached max turns. <cap> Partial result: <partial>` and `make_subagent_additional_kwargs` stamps on `additional_kwargs` — `max_turns_reached` is the one status that carries **both** `subagent_result_brief`/`subagent_result_sha256` (the recovered partial work, like `completed`) and `subagent_error` (the cap notice). The polling loop emits a `task_failed` event so the card transitions out of running; the structured `subagent_status` is the precise reason. The cross-language status contract (`contracts/subagent_status_contract.json` + `subagents/status_contract.py` + `frontend/.../subtask-result.ts`) collapses `max_turns_reached` to the frontend's `failed` pill while the cap detail and recovered work survive on `error`/`result_brief`; the durable delegation ledger prefers the partial `result_brief` and renders model-facing guidance to reuse it, retry with a tighter scope, or raise the per-agent `max_turns`.  `` |
| | `380` | ``+  **Guardrail caps & `stop_reason` (#3875 Phase 2)**: three independent axes can end a subagent run early, and all now surface *why* through one additive field rather than a new status enum. **Turn axis**: `recursion_limit` on the subagent `run_config` equals `max_turns`, so exhausting the turn budget raises `GraphRecursionError` from `agent.astream`; `executor.py::_aexecute` catches it specifically (before the generic `except Exception`). **Token axis**: `TokenBudgetMiddleware` is attached per-agent via `build_subagent_runtime_middlewares` from `subagents.token_budget` (default 2,000,000 tokens, warn at 0.7, hard-stop at 1.0 — a backstop against a subagent that burns tokens on trivial work). It does *not* raise: at the hard-stop threshold it strips the in-flight turn's tool calls, forces `finish_reason="stop"`, and lets the run complete naturally with a final answer. **Loop axis**: `LoopDetectionMiddleware` (attached at the same point) catches repeated identical tool-call sets — or one tool *type* called many times with varying args — and its hard-stop likewise strips `tool_calls` and forces a final answer without raising, recording `loop_capped`. Each guard exposes its cap on a per-`run_id` `consume_stop_reason(run_id)` accessor; `_aexecute` collects **every** middleware with that method (duck-typed via `hasattr`, so the executor has no import coupling to the guard classes) and surfaces the first non-`None` reason — adding a future guard needs no executor change. **Surfacing**: whichever axis fired, `_aexecute` stamps a normal status plus an additive reason — `completed` + `stop_reason=token_capped|turn_capped|loop_capped` when a usable final answer (or partial recovered from the last streamed chunk via `_extract_final_result` → `utils/messages.py::message_content_to_text`, returning a `"No response Generated"` sentinel when no text survived) was produced; `failed` + `stop_reason=turn_capped` when nothing usable survived. `SubagentResult.stop_reason` flows through `task_tool.py::_task_result_command` → `format_subagent_result_message` (renders `Task Succeeded (capped: ...)` / `Task failed (capped: ...)`) and `make_subagent_additional_kwargs`, which stamps the additive `subagent_stop_reason` key alongside the normal `subagent_status`. **Why additive, not an enum**: a new status value would break v1 consumers; an optional field is ignored by older frontends and ledger readers, so the cross-language contract (`contracts/subagent_status_contract.json` v2 + `subagents/status_contract.py` + `frontend/.../subtask-result.ts`, pinned by `test_status_values_match_contract` / `test_stop_reason_values_match_contract`) stays backward-compatible. The durable delegation ledger captures `stop_reason` onto the entry and renders model-facing guidance ("hit a guardrail cap with a partial result; reuse it, retry tighter, or raise the per-agent budget (`max_turns` / `token_budget`)") so the lead reuses a capped completion knowingly instead of mistaking it for a clean one. (Phase 1 shipped this surfacing as a `MAX_TURNS_REACHED` status enum in #3949; Phase 2 replaced that enum with the additive `stop_reason` field per the agreed design — the `max_turns_reached` status value and `SubagentStatus.MAX_TURNS_REACHED` are gone.)  `` |
| `381` | `381` | ``   **Step capture & persistence (#3779)**: `executor.py` captures both assistant turns (`AIMessage`) **and** tool outputs (`ToolMessage`) via `subagents/step_events.py::capture_new_step_messages`, which walks the *newly-appended tail* of each `stream_mode="values"` chunk (not just `messages[-1]`) so a multi-tool-call turn — where LangGraph's `ToolNode` appends several `ToolMessage`s in one super-step — keeps every tool output instead of dropping all but the last. `runtime/runs/worker.py::_SubagentEventBuffer` additionally persists these `task_*` custom events to the `RunEventStore` as `subagent.start`/`subagent.step`/`subagent.end` (`category="subagent"`, `task_id` in `metadata`). It **batches** writes via `put_batch` (flushing on a terminal `subagent.end`, at `FLUSH_THRESHOLD` events, and in the worker's `finally`) rather than one `put()` per step, since `put()` is a documented low-frequency path (per-thread advisory lock per call) and a deep subagent (`max_turns=150`) emits hundreds of steps on the hot stream loop. `build_subagent_step` caps both the per-step `text` and each tool call's serialized `args` at `SUBAGENT_STEP_MAX_CHARS` (flagged `truncated` / `args_truncated`) so a large `write_file`/`bash` payload can't produce an unbounded row. The dedicated category keeps them out of `list_messages` (the thread feed) while `list_events` returns them for the frontend's fetch-on-expand backfill. `list_events` accepts `task_id` (filters on `metadata["task_id"]` — SQL-side in `DbRunEventStore` via `event_metadata["task_id"].as_string()`, in-memory in the JSONL/memory stores) plus an `after_seq` forward cursor, so the card pages through one subagent's steps without the run-wide `limit` truncating the tail (no schema migration: the filter rides the existing run-scoped index). `step_events.py` is a pure, unit-tested layer (`build_subagent_step` / `subagent_run_event`).   `` |
| `382` | `382` | ``   **Deferred MCP tools** (if `tool_search.enabled`): `SubagentExecutor._build_initial_state` assembles deferral after policy filtering via the shared `assemble_deferred_tools` (fail-closed), appends the `tool_search` tool, injects the `<available-deferred-tools>` section into the subagent's `SystemMessage`, and threads the setup to `_create_agent`, which attaches `DeferredToolFilterMiddleware` through `build_subagent_runtime_middlewares(deferred_setup=...)`. Subagents thus withhold full MCP schemas until promotion, same as the lead agent; each task run gets a fresh `ThreadState` so promotion is isolated per run   `` |
| `383` | `383` | ``   **Checkpointer isolation**: Subagent graphs are compiled with `checkpointer=False` to avoid inheriting the parent run's checkpointer, since subagents are one-shot and never resume.   `` |
| 

 |

Collapse file

### [`‎backend/packages/harness/deerflow/agents/middlewares/_bounded_dict.py‎`](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-1daba0335079a7fc8a9ed2b9f783b7bcd68c650180a3d029bdd1b2fd753fae3c)

Copy file name to clipboard

+32Lines changed: 32 additions & 0 deletions

| Original file line number | Diff line number | Diff line change |
| --- | --- | --- |
| 
`   @@ -0,0 +1,32 @@   `

 |
| | `1` | `+  """A small bounded ``OrderedDict`` shared by guard middlewares.  ` |
| | `2` | `+  ` |
| | `3` | `+  Guard middlewares (``TokenBudgetMiddleware``, ``LoopDetectionMiddleware``) keep  ` |
| | `4` | `+  per-``run_id`` state that must not grow without bound on abandoned or reused  ` |
| | `5` | `+  runs. This module provides the single shared implementation so both middlewares  ` |
| | `6` | `+  cap identically and a future guard does not reinvent it.  ` |
| | `7` | `+  """  ` |
| | `8` | `+  ` |
| | `9` | `+  from __future__ import annotations  ` |
| | `10` | `+  ` |
| | `11` | `+  from collections import OrderedDict  ` |
| | `12` | `+  from typing import Any  ` |
| | `13` | `+  ` |
| | `14` | `+  ` |
| | `15` | `+  class BoundedDict(OrderedDict):  ` |
| | `16` | `+  """An ``OrderedDict`` that evicts the oldest entry once ``maxsize`` is reached.  ` |
| | `17` | `+  ` |
| | `18` | `+  Used for per-``run_id`` state (stop-reason flags, pending warnings, usage  ` |
| | `19` | `+  accumulators) so a long-lived middleware instance on the lead agent cannot  ` |
| | `20` | `+  leak memory across many runs. Insertion order is preserved, so the  ` |
| | `21` | `+  least-recently-inserted key is evicted first.  ` |
| | `22` | `+  """  ` |
| | `23` | `+  ` |
| | `24` | `+  def __init__(self, maxsize: int = 1000, *args: Any, **kwds: Any) -> None:  ` |
| | `25` | `+  self.maxsize = maxsize  ` |
| | `26` | `+  super().__init__(*args, **kwds)  ` |
| | `27` | `+  ` |
| | `28` | `+  def __setitem__(self, key: Any, value: Any) -> None:  ` |
| | `29` | `+  if key not in self:  ` |
| | `30` | `+  if len(self) >= self.maxsize:  ` |
| | `31` | `+  self.popitem(last=False)  ` |
| | `32` | `+  super().__setitem__(key, value)  ` |

Collapse file

### [`‎backend/packages/harness/deerflow/agents/middlewares/delegation_ledger.py‎`](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-17f902606bf223d58833acf5f0c0db59ae27a7d6af37971ce8e86be3f53ded51)

Copy file name to clipboardExpand all lines: backend/packages/harness/deerflow/agents/middlewares/delegation\_ledger.py

+14\-4Lines changed: 14 additions & 4 deletions

| Original file line number | Diff line number | Diff line change |
| --- | --- | --- |
| 
`   @@ -50,7 +50,16 @@ def _escape_context_text(value: object) -> str:   `

 |
| `50` | `50` | `   return escape(" ".join(str(value).split()), quote=False)   ` |
| `51` | `51` | `       ` |
| `52` | `52` | `       ` |
| `53` | | `-  def _status_guidance(status: str) -> str:  ` |
| | `53` | `+  def _status_guidance(status: str, stop_reason: str | None = None) -> str:  ` |
| | `54` | `+  if stop_reason:  ` |
| | `55` | `+  # A guardrail cap ended this run early (#3875 Phase 2): the status is  ` |
| | `56` | `+  # still completed/failed, and ``stop_reason`` carries *why* it stopped  ` |
| | `57` | `+  # (token_capped / turn_capped / loop_capped). The old contract surfaced  ` |
| | `58` | `+  # this as a separate ``max_turns_reached`` status; the additive  ` |
| | `59` | `+  # ``stop_reason`` field replaced it so v1 consumers keep working.  ` |
| | `60` | `+  if status == "completed":  ` |
| | `61` | `+  return "hit a guardrail cap with a partial result; reuse the partial result, retry with a tighter scope, or raise the per-agent budget (max_turns / token_budget)"  ` |
| | `62` | `+  return "hit a guardrail cap with no usable result; retry with a tighter scope or raise the per-agent budget (max_turns / token_budget)"  ` |
| `54` | `63` | `   if status == "in_progress":   ` |
| `55` | `64` | `   return "already delegated; do NOT delegate again; wait for or build on the result"   ` |
| `56` | `65` | `   if status == "completed":   ` |
| 

`   @@ -63,8 +72,6 @@ def _status_guidance(status: str) -> str:   `

 |
| `63` | `72` | `   return "timed-out attempt; may retry with a changed plan"   ` |
| `64` | `73` | `   if status == "polling_timed_out":   ` |
| `65` | `74` | `   return "polling timed-out attempt; may retry with a changed plan"   ` |
| `66` | | `-  if status == "max_turns_reached":  ` |
| `67` | | `-  return "hit the turn budget with a partial result; reuse the partial result, retry with a tighter scope, or raise the per-agent max_turns"  ` |
| `68` | `75` | `   return "prior attempt; inspect status before retrying"   ` |
| `69` | `76` | `       ` |
| `70` | `77` | `       ` |
| 

`   @@ -125,6 +132,9 @@ def extract_delegations(messages: list[AnyMessage]) -> list[DelegationEntry]:   `

 |
| `125` | `132` | `   if structured is None:   ` |
| `126` | `133` | `   continue   ` |
| `127` | `134` | `   entry["status"] = structured["status"]   ` |
| | `135` | `+  stop_reason = structured.get("stop_reason")  ` |
| | `136` | `+  if stop_reason:  ` |
| | `137` | `+  entry["stop_reason"] = stop_reason  ` |
| `128` | `138` | `   result_text = structured.get("result_brief") or structured.get("error") or _STATUS_ONLY_RESULT_BRIEFS.get(structured["status"])   ` |
| `129` | `139` | `   if result_text:   ` |
| `130` | `140` | `   result_sha256 = structured.get("result_sha256") or hashlib.sha256(result_text.encode("utf-8")).hexdigest()   ` |
| 

`   @@ -146,7 +156,7 @@ def _render_entry_line(entry: DelegationEntry) -> str:   `

 |
| `146` | `156` | `   status = _escape_context_text(entry["status"])   ` |
| `147` | `157` | `   description = _escape_context_text(entry["description"])   ` |
| `148` | `158` | `   subagent_type = _escape_context_text(entry["subagent_type"])   ` |
| `149` | | `-  guidance = _status_guidance(entry["status"])  ` |
| | `159` | `+  guidance = _status_guidance(entry["status"], entry.get("stop_reason"))  ` |
| `150` | `160` | `   line = f"- [{status}] {description} (via {subagent_type}; {guidance})"   ` |
| `151` | `161` | `   result_brief = entry.get("result_brief")   ` |
| `152` | `162` | `   if result_brief:   ` |
| 

 |

Collapse file

### [`‎backend/packages/harness/deerflow/agents/middlewares/loop_detection_middleware.py‎`](https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83#diff-5dc6e809115603e75eaa4512225adac1a16412200146afd8d173fe739532421f)

Copy file name to clipboardExpand all lines: backend/packages/harness/deerflow/agents/middlewares/loop\_detection\_middleware.py

+47Lines changed: 47 additions & 0 deletions

| Original file line number | Diff line number | Diff line change |
| --- | --- | --- |
| 
`   @@ -36,6 +36,17 @@   `

 |
| `36` | `36` | `   instead of carrying it into a later invocation for the same thread. The   ` |
| `37` | `37` | `   hard-stop path still forces termination when the configured safety limit   ` |
| `38` | `38` | `   is reached.   ` |
| | `39` | `+  ` |
| | `40` | `+  Stop-reason surfacing (#3875 Phase 2):  ` |
| | `41` | `+  Like the token-budget guard, the loop hard stop does NOT raise — it  ` |
| | `42` | `+  strips ``tool_calls`` so the agent loop terminates naturally with a  ` |
| | `43` | `+  final answer. To let the caller (the subagent executor) distinguish a  ` |
| | `44` | `+  loop-capped completion from a clean one, the run that triggered the hard  ` |
| | `45` | `+  stop is recorded in ``_stop_reason`` and exposed via  ` |
| | `46` | ``+  :meth:`consume_stop_reason`. The executor collects that reason alongside  `` |
| | `47` | `+  the token-budget guard's so a loop-capped run surfaces as  ` |
| | `48` | `+  ``completed + loop_capped`` and the lead/ledger can tell it was capped  ` |
| | `49` | `+  without parsing result text.  ` |
| `39` | `50` | `   """   ` |
| `40` | `51` | `       ` |
| `41` | `52` | `   from __future__ import annotations   ` |
| 

`   @@ -55,6 +66,8 @@   `

 |
| `55` | `66` | `   from langchain_core.messages import HumanMessage   ` |
| `56` | `67` | `   from langgraph.runtime import Runtime   ` |
| `57` | `68` | `       ` |
| | `69` | `+  from deerflow.agents.middlewares._bounded_dict import BoundedDict  ` |
| | `70` | `+  ` |
| `58` | `71` | `   if TYPE_CHECKING:   ` |
| `59` | `72` | `   from deerflow.config.loop_detection_config import LoopDetectionConfig   ` |
| `60` | `73` | `       ` |
| 

`   @@ -231,6 +244,14 @@ def __init__(   `

 |
| `231` | `244` | `   self._pending_warnings: dict[tuple[str, str], list[str]] = defaultdict(list)   ` |
| `232` | `245` | `   self._pending_warning_touch_order: OrderedDict[tuple[str, str], None] = OrderedDict()   ` |
| `233` | `246` | `   self._max_pending_warning_keys = max(1, self.max_tracked_threads * 2)   ` |
| | `247` | `+  # Stop reason set when a hard-stop fires (#3875 Phase 2). Keyed by run_id  ` |
| | `248` | `+  # (matching ``TokenBudgetMiddleware``) and bounded — the lead agent's  ` |
| | `249` | `+  # middleware instance is long-lived across many runs, so without a cap  ` |
| | `250` | `+  # an entry would accumulate for every looped lead run. Intentionally NOT  ` |
| | `251` | `+  # cleared by ``after_agent``/``_clear_current_run_pending_warnings`` so  ` |
| | `252` | `+  # the subagent executor can consume it after the run returns; ``reset()``  ` |
| | `253` | `+  # still drops it.  ` |
| | `254` | `+  self._stop_reason: BoundedDict[str, str] = BoundedDict(1000)  ` |
| `234` | `255` | `       ` |
| `235` | `256` | `   @classmethod   ` |
| `236` | `257` | `   def from_config(cls, config: LoopDetectionConfig) -> LoopDetectionMiddleware:   ` |
| 

`   @@ -259,6 +280,20 @@ def _get_run_id(self, runtime: Runtime) -> str:   `

 |
| `259` | `280` | `   return str(run_id)   ` |
| `260` | `281` | `   return "default"   ` |
| `261` | `282` | `       ` |
| | `283` | `+  def consume_stop_reason(self, run_id: str | None) -> str | None:  ` |
| | `284` | `+  """Pop and return the stop reason the hard-stop set for this run.  ` |
| | `285` | `+  ` |
| | `286` | `+  Returns ``"loop_capped"`` when a repeated tool-call loop tripped the hard  ` |
| | `287` | `+  stop during the run — the run still completed with a forced final answer  ` |
| | `288` | `+  (the hard stop strips ``tool_calls`` rather than raising). The subagent  ` |
| | `289` | `+  executor calls this after the run returns so a loop-capped completion  ` |
| | `290` | `+  carries ``stop_reason=loop_capped`` to the lead instead of looking like  ` |
| | `291` | `+  a clean ``completed``. Mirrors ``TokenBudgetMiddleware.consume_stop_reason``;  ` |
| | `292` | `+  popping keeps the dict from accumulating on a reused instance.  ` |
| | `293` | `+  """  ` |
| | `294` | `+  with self._lock:  ` |
| | `295` | `+  return self._stop_reason.pop(run_id, None)  ` |
| | `296` | `+  ` |
| `262` | `297` | `   def _pending_key(self, runtime: Runtime) -> tuple[str, str]:   ` |
| `263` | `298` | `   """Return the pending-warning key for the current thread/run."""   ` |
| `264` | `299` | `   return self._get_thread_id(runtime), self._get_run_id(runtime)   ` |
| 

`   @@ -478,6 +513,17 @@ def _apply(self, state: AgentState, runtime: Runtime) -> dict | None:   `

 |
| `478` | `513` | `   warning, hard_stop = self._track_and_check(state, runtime)   ` |
| `479` | `514` | `       ` |
| `480` | `515` | `   if hard_stop:   ` |
| | `516` | `+  # Record the stop reason so the executor can surface  ` |
| | `517` | `+  # ``stop_reason=loop_capped`` after the run returns (#3875 Phase 2).  ` |
| | `518` | `+  # The hard stop does not raise — it strips tool_calls and lets the  ` |
| | `519` | `+  # run finish with a forced final answer — so without this the caller  ` |
| | `520` | `+  # would see a clean ``completed``. See ``consume_stop_reason``.  ` |
| | `521` | `+  # Written under the lock to match ``TokenBudgetMiddleware``: the lead  ` |
| | `522` | `+  # agent's middleware instance is shared across concurrent Gateway  ` |
| | `523` | `+  # threads, so the bounded-dict write needs the same guard.  ` |
| | `524` | `+  run_id = self._get_run_id(runtime)  ` |
| | `525` | `+  with self._lock:  ` |
| | `526` | `+  self._stop_reason[run_id] = "loop_capped"  ` |
| `481` | `527` | `   # Strip tool_calls from the last AIMessage to force text output.   ` |
| `482` | `528` | `   # Once tool_calls are stripped, the AIMessage no longer requires   ` |
| `483` | `529` | `   # matching ToolMessage responses, so mutating it in place here   ` |
| 

`   @@ -610,3 +656,4 @@ def reset(self, thread_id: str | None = None) -> None:   `

 |
| `610` | `656` | `   self._tool_freq_warned.clear()   ` |
| `611` | `657` | `   self._pending_warnings.clear()   ` |
| `612` | `658` | `   self._pending_warning_touch_order.clear()   ` |
| | `659` | `+  self._stop_reason.clear()  ` |

## 0 commit comments

Comments

0 (0)

Please [sign in](https://github.com/login?return_to=https://github.com/bytedance/deer-flow/commit/c9fb9768d476e28de0294ac7a23cab9819b93f83) to comment.