# Source: https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0

### Uh oh!

There was an error while loading. [Please reload this page]().

[bytedance](https://github.com/bytedance) / **[deer-flow](https://github.com/bytedance/deer-flow)** Public

- [Notifications](https://github.com/login?return_to=%2Fbytedance%2Fdeer-flow) You must be signed in to change notification settings
- [Fork 10.9k](https://github.com/login?return_to=%2Fbytedance%2Fdeer-flow)
- [Star 79.8k](https://github.com/login?return_to=%2Fbytedance%2Fdeer-flow)

 ## File tree

Expand file treeCollapse file tree

TopOpen diff view settings

Filter options

- [README.md](https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0#diff-b335630551682c19a781afebcf4d07bf978fb1f8ac04c6bf87428ed5106870f5)

- backend

 - [AGENTS.md](https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0#diff-2c0330701aa3b374d2869cd99dc511b7220af9fb606a7981982d8ce5bc247554)

 - packages/harness/deerflow

 - community/aio\_sandbox

 - [aio\_sandbox\_provider.py](https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0#diff-0f519f9cc378a7b0564088bd14b27fab2614cfb0b75096c9d9a6436e61354f3b)

 - [remote\_backend.py](https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0#diff-bd92d0b0bc3d08ee2091071692c2c0b4f9a0fd1404c554767e2e8c36db2353c0)

 - integrations

 - [lark\_cli.py](https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0#diff-77cc1e991f595c29b08b03188719d415970faafda88c690a29bda8848a73f06b)

 - tests

 - [test\_aio\_sandbox\_provider.py](https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0#diff-35b569e1db0ec1fb9dca9b23f829b7a40d3e9709f24080c63cf2769588e0781e)

 - [test\_lark\_cli\_integration.py](https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0#diff-33b993b2c3c332fb8a828161d3090f34ce07402552cb791b179d7536cf5c8fc3)

 - [test\_provisioner\_mount\_contract.py](https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0#diff-f3c542468dca7adcaa74b032c5fe70b6d53915ce921d376fa77a4f45f9201500)

 - [test\_provisioner\_pvc\_volumes.py](https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0#diff-b535126ad90ad1116eba0ad347a72a028201a8e79e18ac86c0722f4e94a9b1ef)

- docker/provisioner

 - [app.py](https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0#diff-58d8fa3c8a63efdb78f966f7760a492f315153a3cb1c2838870377ee550fe0cd)

Expand file treeCollapse file tree

Top

Open diff view settings

Collapse file

### [`‎README.md‎`](https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0#diff-b335630551682c19a781afebcf4d07bf978fb1f8ac04c6bf87428ed5106870f5)

Copy file name to clipboardExpand all lines: README.md

+4\-2Lines changed: 4 additions & 2 deletions

- Display the source diff
- Display the rich diff

| Original file line number | Diff line number | Diff line change |
| --- | --- | --- |
| 
``   @@ -791,8 +791,10 @@ set `DEER_FLOW_LARK_CLI_SANDBOX_RUNTIME_DIR` to that directory.   ``

 |
| `791` | `791` | `   > **Sandbox trust boundary:** the browser never receives the Lark app secret, but   ` |
| `792` | `792` | ``   > agent conversations run `lark-cli` inside the sandbox, so the per-user   `` |
| `793` | `793` | ``   > credential directories are mounted into it: `config` (holding the long-lived   `` |
| `794` | | ``-  > `appSecret`) is mounted **read-only** and `data` (refreshable OAuth tokens)  `` |
| `795` | | `-  > writable. Both remain *readable* by any process the agent runs there, so code  ` |
| | `794` | ``+  > `appSecret`) is mounted **read-only**, its otherwise empty `config/locks`  `` |
| | `795` | ``+  > subdirectory is over-mounted writable for `lark-cli` coordination files, and  `` |
| | `796` | ``+  > `data` (refreshable OAuth tokens) is writable. The credential-bearing config  `` |
| | `797` | `+  > and data mounts remain *readable* by any process the agent runs there, so code  ` |
| `796` | `798` | `   > reached via prompt injection in a tool result could read them. Treat the   ` |
| `797` | `799` | `   > sandbox as inside the Lark credential trust boundary until the sidecar   ` |
| `798` | `800` | `   > credential-broker follow-up removes these mounts from sandbox execution.   ` |
| 

 |

Collapse file

### [`‎backend/AGENTS.md‎`](https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0#diff-2c0330701aa3b374d2869cd99dc511b7220af9fb606a7981982d8ce5bc247554)

Copy file name to clipboardExpand all lines: backend/AGENTS.md

+1\-1Lines changed: 1 addition & 1 deletion

- Display the source diff
- Display the rich diff

| Original file line number | Diff line number | Diff line change |
| --- | --- | --- |
| 
`   @@ -877,7 +877,7 @@ E2B output sync records remote file versions and actual host file metadata in a   `

 |
| `877` | `877` | ``   - `skills/describe.py` — `build_describe_skill_tool(catalog)` builds the `describe_skill` tool as a closure; `build_skill_search_setup(skills, enabled, ...)` produces a `SkillSearchSetup(describe_skill_tool, skill_names)` that is wired into both the LangGraph agent factory (`agent.py`) and the embedded client (`client.py`).   `` |
| `878` | `878` | ``   - **Slash activation**: `/skill-name task` loads that enabled skill's `SKILL.md` for the current model call only. The resolver rejects leading whitespace, missing separators, reserved channel commands (`/new`, `/help`, `/bootstrap`, `/status`, `/models`, `/memory`, `/goal`), disabled skills, and skills outside a custom agent's whitelist.   `` |
| `879` | `879` | ``   - **Installation**: `POST /api/skills/install` extracts .skill ZIP archive to custom/ directory   `` |
| `880` | | ``-  - **Managed integrations**: Lark/Feishu CLI support installs one global official `lark-*` pack as read-only `SkillCategory.INTEGRATION` entries under `/mnt/skills/integrations/lark-cli/...`; enabled flags, app configuration, and OAuth data remain per-user. Install resolves the newest `larksuite/cli` release from GitHub (`releases/latest`) at install time (falling back to a bottom-line pinned version if the lookup fails) rather than hard-coding the pack version; integrity relies on the official host + structural archive guards + a recorded hash of the effective installed tree after shared guidance injection (not a pinned archive-byte SHA, which GitHub does not keep stable). The Gateway image still installs a pinned `@larksuite/cli` binary, so `get_lark_integration_status` surfaces `latest_available_version` and `runtime_version_mismatch` for the UI. AIO installs additionally verify and publish official Linux amd64/arm64 binaries under `{DEER_FLOW_HOME}/integrations/lark-cli/sandbox-cli`, mounted read-only at `/mnt/integrations/lark-cli/runtime`; `/mnt/integrations/lark-cli/config` (app credentials, incl. the long-lived `appSecret`) is mounted **read-only** into the sandbox and `/mnt/integrations/lark-cli/data` (refreshable OAuth tokens) stays writable, both mapping to owner-only per-user credential directories. **Sandbox trust boundary:** those two dirs are still *readable* by arbitrary sandbox processes (the agent's `bash` tool, or code reached via prompt-injection in a tool result), so the app secret and tokens are exposed to sandbox-side code even though they never reach the browser — the read-only config mount only prevents in-sandbox tampering, not read/exfiltration. The sidecar credential-broker (Pattern B, issue #4338) is the fix that removes these plaintext mounts from sandbox execution: set `LARK_CLI_BROKER_IMAGE` on the provisioner (see `docker/lark-cli-broker/`) and the Gateway sends `provision_lark_cli_broker` on sandbox create. The provisioner then runs a `lark-cli-broker` sidecar that owns the per-user `config`/`data` (mounted into the **sidecar only**, at `/var/lark/{config,data}`) and serves the `lark-cli` command surface on Pod loopback (`http://127.0.0.1:8788`); a shim init container (`install-shim`) writes a forwarding `lark-cli` into the shared runtime `emptyDir`, so the sandbox gets `DEERFLOW_LARK_BROKER_URL` + a shim on PATH but **no** credential files. The on-PATH `bin/lark-cli` is a `/bin/sh` launcher that resolves a Python 3 interpreter and execs the Python shim body (`bin/lark-cli-shim.py`) beside it, so broker mode does not silently ENOEXEC on a sandbox image without a `#!/usr/bin/env python3`-resolvable interpreter — it fails loudly (exit 127, actionable message) and can be pinned with `DEERFLOW_LARK_BROKER_PYTHON`. The broker runs `lark-cli` in the sidecar's cwd and cannot see the sandbox filesystem, so cwd is intentionally **not** forwarded and file-I/O subcommands relative to the sandbox cwd are unsupported (command surface only). An optional `DEERFLOW_LARK_BROKER_DENY_SUBCOMMANDS` denylist (comma-separated command prefixes, forwarded from the provisioner) lets the broker refuse secret-dumping subcommands before spawning the binary. `lark_cli_env_overlay(broker=True)` therefore omits `LARKSUITE_CLI_CONFIG_DIR`/`DATA_DIR`; `sandbox_lark_broker_active()` (TTL-cached provisioner `/api/capabilities` probe, tight timeout + longer negative caching on the bash hot path) selects broker vs. binary mode for both the bash env overlay and status. `DEER_FLOW_LARK_CLI_SANDBOX_RUNTIME_DIR` supplies a validated, symlink-free pre-staged runtime for air-gapped deployments. For the remote provisioner (K8s), the runtime binary is otherwise provisioned by an optional init container + shared `emptyDir` (Pattern A): set `LARK_CLI_INIT_IMAGE` on the provisioner (see `docker/lark-cli-init/`) and the Gateway sends `provision_lark_cli_runtime` on sandbox create once the pack is installed, so remote installs skip the Gateway-side GitHub download entirely. Broker (Pattern B) supersedes the init-container binary (Pattern A) when both images are configured. `get_lark_integration_status(check_runtime=True)` surfaces `sandbox_runtime_mode` (`none` / `gateway-download` / `init-container` / `broker`) and `sandbox_runtime_ready` (remote modes read the provisioner `GET /api/capabilities`: `lark_cli_init_image` / `lark_cli_broker_image`) so a green UI can't hide a chat-time `lark-cli: command not found`. Cheap status probes are explicitly not live-verified; users authorize or reconnect through the browser device-flow endpoints instead of running terminal commands.  `` |
| | `880` | ``+  - **Managed integrations**: Lark/Feishu CLI support installs one global official `lark-*` pack as read-only `SkillCategory.INTEGRATION` entries under `/mnt/skills/integrations/lark-cli/...`; enabled flags, app configuration, and OAuth data remain per-user. Install resolves the newest `larksuite/cli` release from GitHub (`releases/latest`) at install time (falling back to a bottom-line pinned version if the lookup fails) rather than hard-coding the pack version; integrity relies on the official host + structural archive guards + a recorded hash of the effective installed tree after shared guidance injection (not a pinned archive-byte SHA, which GitHub does not keep stable). The Gateway image still installs a pinned `@larksuite/cli` binary, so `get_lark_integration_status` surfaces `latest_available_version` and `runtime_version_mismatch` for the UI. AIO installs additionally verify and publish official Linux amd64/arm64 binaries under `{DEER_FLOW_HOME}/integrations/lark-cli/sandbox-cli`, mounted read-only at `/mnt/integrations/lark-cli/runtime`; `/mnt/integrations/lark-cli/config` (app credentials, incl. the long-lived `appSecret`) is mounted **read-only** into the sandbox, its empty `config/locks` subdirectory is over-mounted writable for `lark-cli` coordination files, and `/mnt/integrations/lark-cli/data` (refreshable OAuth tokens) stays writable, all mapping to owner-only per-user directories. **Sandbox trust boundary:** the credential-bearing config and data dirs are still *readable* by arbitrary sandbox processes (the agent's `bash` tool, or code reached via prompt-injection in a tool result), so the app secret and tokens are exposed to sandbox-side code even though they never reach the browser — the read-only config mount only prevents in-sandbox tampering, not read/exfiltration. The sidecar credential-broker (Pattern B, issue #4338) is the fix that removes these plaintext mounts from sandbox execution: set `LARK_CLI_BROKER_IMAGE` on the provisioner (see `docker/lark-cli-broker/`) and the Gateway sends `provision_lark_cli_broker` on sandbox create. The provisioner then runs a `lark-cli-broker` sidecar that owns the per-user `config`/`config/locks`/`data` mounts (mounted into the **sidecar only**, at `/var/lark/{config,config/locks,data}` with only the nested locks mount writable) and serves the `lark-cli` command surface on Pod loopback (`http://127.0.0.1:8788`); a shim init container (`install-shim`) writes a forwarding `lark-cli` into the shared runtime `emptyDir`, so the sandbox gets `DEERFLOW_LARK_BROKER_URL` + a shim on PATH but **no** credential files. The on-PATH `bin/lark-cli` is a `/bin/sh` launcher that resolves a Python 3 interpreter and execs the Python shim body (`bin/lark-cli-shim.py`) beside it, so broker mode does not silently ENOEXEC on a sandbox image without a `#!/usr/bin/env python3`-resolvable interpreter — it fails loudly (exit 127, actionable message) and can be pinned with `DEERFLOW_LARK_BROKER_PYTHON`. The broker runs `lark-cli` in the sidecar's cwd and cannot see the sandbox filesystem, so cwd is intentionally **not** forwarded and file-I/O subcommands relative to the sandbox cwd are unsupported (command surface only). An optional `DEERFLOW_LARK_BROKER_DENY_SUBCOMMANDS` denylist (comma-separated command prefixes, forwarded from the provisioner) lets the broker refuse secret-dumping subcommands before spawning the binary. `lark_cli_env_overlay(broker=True)` therefore omits `LARKSUITE_CLI_CONFIG_DIR`/`DATA_DIR`; `sandbox_lark_broker_active()` (TTL-cached provisioner `/api/capabilities` probe, tight timeout + longer negative caching on the bash hot path) selects broker vs. binary mode for both the bash env overlay and status. `DEER_FLOW_LARK_CLI_SANDBOX_RUNTIME_DIR` supplies a validated, symlink-free pre-staged runtime for air-gapped deployments. For the remote provisioner (K8s), the runtime binary is otherwise provisioned by an optional init container + shared `emptyDir` (Pattern A): set `LARK_CLI_INIT_IMAGE` on the provisioner (see `docker/lark-cli-init/`) and the Gateway sends `provision_lark_cli_runtime` on sandbox create once the pack is installed, so remote installs skip the Gateway-side GitHub download entirely. Broker (Pattern B) supersedes the init-container binary (Pattern A) when both images are configured. `get_lark_integration_status(check_runtime=True)` surfaces `sandbox_runtime_mode` (`none` / `gateway-download` / `init-container` / `broker`) and `sandbox_runtime_ready` (remote modes read the provisioner `GET /api/capabilities`: `lark_cli_init_image` / `lark_cli_broker_image`) so a green UI can't hide a chat-time `lark-cli: command not found`. Cheap status probes are explicitly not live-verified; users authorize or reconnect through the browser device-flow endpoints instead of running terminal commands.  `` |
| `881` | `881` | ``   - **SkillScan**: `packages/harness/deerflow/skills/skillscan/` is the native deterministic scanner for `.skill` archives and agent-managed skill writes. It runs offline before the LLM scanner, emits structured findings (`rule_id`, `severity`, `file`, `line`, `message`, `remediation`, redacted `evidence` — category/analyzer are encoded in the `rule_id` prefix), blocks `CRITICAL`, and passes warning findings into `scan_skill_content()`. `scan_archive_preflight()` / `scan_skill_dir()` are pure sync functions (dispatch off the event loop); `enforce_static_scan()` applies the blocking policy and the `skill_scan.enabled` kill switch. The Python instance-client signal deliberately follows only a one-level, same-scope evidence chain (PR #4265 review): a proven imported constructor bound to a simple name, optional name-to-name alias propagation, rebinding invalidation, and a constructor-supported outbound method or context-manager use; bare canonical-looking names never fall back to module identity. Nested scopes never inherit client handles and inherit only constructor aliases proven stable by a binding-only enclosing-scope prepass. Comprehensions, walrus-bearing statements, annotations, executable expressions inside complex binding targets, unsupported operations, and ambiguous flows produce no finding from this signal; skipped constructs invalidate all names they may bind, while representative false negatives are pinned by `test_python_declared_false_negatives_stay_unreported`. Compound bodies are walked from isolated copies so wrapping code in `if True:` is not a bypass, while copied scope entries, binding-only prepasses, and AST visits consume a deterministic work budget and the walk stops after its first sink. Budget or recursion exhaustion skips only this best-effort signal and retains deterministic findings already collected for the file. Do not add Semgrep/OpenGrep or YAML rule-engine dependencies to the core path; Phase 1 rule specs live in Python constants next to their analyzers in `skillscan/orchestrator.py`.   `` |
| `882` | `882` | ``   - **Skill Review Core**: `packages/harness/deerflow/skills/review/` provides read-only package snapshots, deterministic facts, resource/eval analysis, report rendering, and the CLI (`python -m deerflow.skills.review.cli`). It reuses the shared frontmatter helper and SkillScan; it must not import `app.*`, execute target scripts, install dependencies, or call networks. JSON contracts live in `contracts/skill_review/`. The `review_skill_package` built-in tool labels results with `review_subject_entry` and never `skill_context_entry`, so reviewing a target does not activate it, bind its `required-secrets`, or apply its `allowed-tools`. Its model-visible `ToolMessage.content` is a compact JSON payload with untrusted control tags neutralized; the full raw review payload, including Markdown renders, stays in `ToolMessage.artifact`. CI should run the CLI with `--fail-on error --fail-on-incomplete` so blocker/error findings and truncated/not-assessed packages fail the gate. The public `skills/public/skill-reviewer` skill owns semantic readiness review and suggestions only; mutation and runtime experiments remain owned by `skill-creator`.   `` |
| `883` | `883` | `       ` |
| 

 |

Collapse file

### [`‎backend/packages/harness/deerflow/community/aio_sandbox/aio_sandbox_provider.py‎`](https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0#diff-0f519f9cc378a7b0564088bd14b27fab2614cfb0b75096c9d9a6436e61354f3b)

Copy file name to clipboardExpand all lines: backend/packages/harness/deerflow/community/aio\_sandbox/aio\_sandbox\_provider.py

+9\-4Lines changed: 9 additions & 4 deletions

| Original file line number | Diff line number | Diff line change |
| --- | --- | --- |
| 
`   @@ -40,7 +40,7 @@   `

 |
| `40` | `40` | `   from deerflow.config import get_app_config   ` |
| `41` | `41` | `   from deerflow.config.paths import VIRTUAL_PATH_PREFIX, get_paths, join_host_path   ` |
| `42` | `42` | `   from deerflow.integrations.lark_cli import INTEGRATION_ID as LARK_CLI_INTEGRATION_ID   ` |
| `43` | | `-  from deerflow.integrations.lark_cli import LARK_CLI_SANDBOX_CONFIG_DIR, LARK_CLI_SANDBOX_DATA_DIR, LARK_CLI_SANDBOX_RUNTIME_DIR, ensure_lark_cli_credential_tree, lark_skills_installed  ` |
| | `43` | `+  from deerflow.integrations.lark_cli import LARK_CLI_SANDBOX_CONFIG_DIR, LARK_CLI_SANDBOX_DATA_DIR, LARK_CLI_SANDBOX_LOCKS_DIR, LARK_CLI_SANDBOX_RUNTIME_DIR, ensure_lark_cli_credential_tree, lark_skills_installed  ` |
| `44` | `44` | `   from deerflow.runtime.user_context import get_effective_user_id   ` |
| `45` | `45` | `   from deerflow.sandbox.sandbox import Sandbox   ` |
| `46` | `46` | `   from deerflow.sandbox.sandbox_provider import SandboxProvider   ` |
| 

`   @@ -1019,8 +1019,11 @@ def _get_lark_cli_runtime_mounts(*, user_id: str | None = None) -> list[tuple[st   `

 |
| `1019` | `1019` | `   ``lark-cli config init`` on the Gateway, never in-sandbox), so it is   ` |
| `1020` | `1020` | `   mounted **read-only**: sandbox processes only need to read it, and a   ` |
| `1021` | `1021` | `   read-only bind stops a compromised agent from tampering with or   ` |
| `1022` | | `-  replacing the app credentials. The ``data`` dir holds refreshable OAuth  ` |
| `1023` | | `-  tokens that ``lark-cli auth`` updates in-sandbox, so it stays writable.  ` |
| | `1022` | `+  replacing the app credentials. Newer ``lark-cli`` versions coordinate  ` |
| | `1023` | `+  API calls through ``config/locks``, so that empty subdirectory is  ` |
| | `1024` | `+  over-mounted writable without exposing the rest of ``config`` to  ` |
| | `1025` | `+  writes. The ``data`` dir holds refreshable OAuth tokens that  ` |
| | `1026` | `+  ``lark-cli auth`` updates in-sandbox, so it stays writable.  ` |
| `1024` | `1027` | `   This is defense-in-depth only — both dirs remain readable to arbitrary   ` |
| `1025` | `1028` | `   sandbox processes until the auth-proxy follow-up (issue #4338) lands.   ` |
| `1026` | `1029` | `   See the sandbox trust-boundary note in ``backend/AGENTS.md``.   ` |
| 

`   @@ -1029,8 +1032,10 @@ def _get_lark_cli_runtime_mounts(*, user_id: str | None = None) -> list[tuple[st   `

 |
| `1029` | `1032` | `   paths = get_paths()   ` |
| `1030` | `1033` | `   effective_user_id = AioSandboxProvider._effective_acquire_user_id(user_id)   ` |
| `1031` | `1034` | `   ensure_lark_cli_credential_tree(effective_user_id, paths=paths)   ` |
| | `1035` | `+  config_dir = paths.host_user_integration_config_dir(effective_user_id, LARK_CLI_INTEGRATION_ID)  ` |
| `1032` | `1036` | `   mounts = [   ` |
| `1033` | | `-  (paths.host_user_integration_config_dir(effective_user_id, LARK_CLI_INTEGRATION_ID), LARK_CLI_SANDBOX_CONFIG_DIR, True),  ` |
| | `1037` | `+  (config_dir, LARK_CLI_SANDBOX_CONFIG_DIR, True),  ` |
| | `1038` | `+  (join_host_path(config_dir, "locks"), LARK_CLI_SANDBOX_LOCKS_DIR, False),  ` |
| `1034` | `1039` | `   (paths.host_user_integration_data_dir(effective_user_id, LARK_CLI_INTEGRATION_ID), LARK_CLI_SANDBOX_DATA_DIR, False),   ` |
| `1035` | `1040` | `   ]   ` |
| `1036` | `1041` | `   runtime_dir = paths.base_dir / "integrations" / LARK_CLI_INTEGRATION_ID / "sandbox-cli"   ` |
| 

 |

Collapse file

### [`‎backend/packages/harness/deerflow/community/aio_sandbox/remote_backend.py‎`](https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0#diff-bd92d0b0bc3d08ee2091071692c2c0b4f9a0fd1404c554767e2e8c36db2353c0)

Copy file name to clipboardExpand all lines: backend/packages/harness/deerflow/community/aio\_sandbox/remote\_backend.py

+9\-6Lines changed: 9 additions & 6 deletions

| Original file line number | Diff line number | Diff line change |
| --- | --- | --- |
| 
`   @@ -34,6 +34,7 @@   `

 |
| `34` | `34` | `   "/mnt/skills/custom",   ` |
| `35` | `35` | `   "/mnt/skills/integrations",   ` |
| `36` | `36` | `   "/mnt/integrations/lark-cli/config",   ` |
| | `37` | `+  "/mnt/integrations/lark-cli/config/locks",  ` |
| `37` | `38` | `   "/mnt/integrations/lark-cli/data",   ` |
| `38` | `39` | `   "/mnt/integrations/lark-cli/runtime",   ` |
| `39` | `40` | `   }   ` |
| 

`   @@ -54,15 +55,17 @@ def _provisioner_extra_mounts_payload(   `

 |
| `54` | `55` | `   When ``provision_lark_cli_runtime`` is set, the provisioner supplies the   ` |
| `55` | `56` | `   lark-cli runtime via an init container + emptyDir, so the runtime extra mount   ` |
| `56` | `57` | `   is dropped here to avoid a colliding hostPath/PVC mount at the same path. The   ` |
| `57` | | `-  per-user config/data credential mounts are still forwarded (they are mounted  ` |
| `58` | | `-  into the sandbox in Pattern A).  ` |
| | `58` | `+  per-user config/locks/data mounts are still forwarded (they are mounted into  ` |
| | `59` | `+  the sandbox in Pattern A). The config root remains read-only while its  ` |
| | `60` | `+  nested locks mount is writable for lark-cli's coordination files.  ` |
| `59` | `61` | |
| `60` | `62` | `   When ``provision_lark_cli_broker`` is set (Pattern B, issue #4338), the   ` |
| `61` | `63` | `   provisioner runs a broker sidecar that holds the credentials, so the   ` |
| `62` | | `-  config/data mounts are **forwarded** (the provisioner wires them into the  ` |
| `63` | | `-  sidecar, not the sandbox) while the runtime mount is dropped. Nothing changes  ` |
| `64` | | `-  in this payload beyond keeping config/data available for the provisioner to  ` |
| `65` | | `-  place; the runtime entry is dropped in both modes.  ` |
| | `64` | `+  config/locks/data mounts are **forwarded** (the provisioner wires them into  ` |
| | `65` | `+  the sidecar, not the sandbox) while the runtime mount is dropped. Nothing  ` |
| | `66` | `+  changes in this payload beyond keeping those credential-related mounts  ` |
| | `67` | `+  available for the provisioner to place; the runtime entry is dropped in  ` |
| | `68` | `+  both modes.  ` |
| `66` | `69` | `   """   ` |
| `67` | `70` | `   if not extra_mounts:   ` |
| `68` | `71` | `   return []   ` |
| 

 |

Collapse file

### [`‎backend/packages/harness/deerflow/integrations/lark_cli.py‎`](https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0#diff-77cc1e991f595c29b08b03188719d415970faafda88c690a29bda8848a73f06b)

Copy file name to clipboardExpand all lines: backend/packages/harness/deerflow/integrations/lark\_cli.py

+2\-1Lines changed: 2 additions & 1 deletion

| Original file line number | Diff line number | Diff line change |
| --- | --- | --- |
| 
`   @@ -106,6 +106,7 @@   `

 |
| `106` | `106` | `   LARK_CLI_MAX_RUNTIME_ASSET_BYTES = 128 * 1024 * 1024   ` |
| `107` | `107` | `   LARK_CLI_MANIFEST_FILE = ".deerflow-lark-cli-manifest.json"   ` |
| `108` | `108` | `   LARK_CLI_SANDBOX_CONFIG_DIR = "/mnt/integrations/lark-cli/config"   ` |
| | `109` | `+  LARK_CLI_SANDBOX_LOCKS_DIR = f"{LARK_CLI_SANDBOX_CONFIG_DIR}/locks"  ` |
| `109` | `110` | `   LARK_CLI_SANDBOX_DATA_DIR = "/mnt/integrations/lark-cli/data"   ` |
| `110` | `111` | `   LARK_CLI_SANDBOX_RUNTIME_DIR = "/mnt/integrations/lark-cli/runtime"   ` |
| `111` | `112` | `   LARK_CLI_LINUX_ARCHES = ("amd64", "arm64")   ` |
| 

`   @@ -309,7 +310,7 @@ def ensure_lark_cli_credential_tree(user_id: str, *, paths: Paths | None = None)   `

 |
| `309` | `310` | `   raise ValueError(f"Lark CLI credential path must not be a symlink: {root}")   ` |
| `310` | `311` | `   root.mkdir(parents=True, exist_ok=True, mode=0o700)   ` |
| `311` | `312` | `   root.chmod(0o700)   ` |
| `312` | | `-  for required in (root / "config", root / "data"):  ` |
| | `313` | `+  for required in (root / "config", root / "config" / "locks", root / "data"):  ` |
| `313` | `314` | `   if required.is_symlink():   ` |
| `314` | `315` | `   raise ValueError(f"Lark CLI credential path must not be a symlink: {required}")   ` |
| `315` | `316` | `   required.mkdir(parents=True, exist_ok=True, mode=0o700)   ` |
| 

 |

Collapse file

### [`‎backend/tests/test_aio_sandbox_provider.py‎`](https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0#diff-35b569e1db0ec1fb9dca9b23f829b7a40d3e9709f24080c63cf2769588e0781e)

Copy file name to clipboardExpand all lines: backend/tests/test\_aio\_sandbox\_provider.py

+10Lines changed: 10 additions & 0 deletions

| Original file line number | Diff line number | Diff line change |
| --- | --- | --- |
| 
`   @@ -183,17 +183,24 @@ def test_get_lark_cli_runtime_mounts_uses_user_auth_dirs(tmp_path, monkeypatch):   `

 |
| `183` | `183` | `   runtime_dir.mkdir(parents=True)   ` |
| `184` | `184` | `       ` |
| `185` | `185` | `   mounts = aio_mod.AioSandboxProvider._get_lark_cli_runtime_mounts(user_id="alice")   ` |
| | `186` | `+  mount_order = [container_path for _host_path, container_path, _read_only in mounts]  ` |
| `186` | `187` | `   container_paths = {container_path: (host_path, read_only) for host_path, container_path, read_only in mounts}   ` |
| `187` | `188` | `       ` |
| `188` | `189` | `   assert container_paths[lark_cli.LARK_CLI_SANDBOX_CONFIG_DIR] == (   ` |
| `189` | `190` | `   str(tmp_path / "users" / "alice" / "integrations" / "lark-cli" / "config"),   ` |
| `190` | `191` | `   True,   ` |
| `191` | `192` | `   )   ` |
| | `193` | `+  assert container_paths[f"{lark_cli.LARK_CLI_SANDBOX_CONFIG_DIR}/locks"] == (  ` |
| | `194` | `+  str(tmp_path / "users" / "alice" / "integrations" / "lark-cli" / "config" / "locks"),  ` |
| | `195` | `+  False,  ` |
| | `196` | `+  )  ` |
| | `197` | `+  assert mount_order.index(lark_cli.LARK_CLI_SANDBOX_CONFIG_DIR) < mount_order.index(lark_cli.LARK_CLI_SANDBOX_LOCKS_DIR)  ` |
| `192` | `198` | `   assert container_paths[lark_cli.LARK_CLI_SANDBOX_DATA_DIR] == (   ` |
| `193` | `199` | `   str(tmp_path / "users" / "alice" / "integrations" / "lark-cli" / "data"),   ` |
| `194` | `200` | `   False,   ` |
| `195` | `201` | `   )   ` |
| `196` | `202` | `   assert stat.S_IMODE((tmp_path / "users" / "alice" / "integrations" / "lark-cli" / "config").stat().st_mode) == 0o700   ` |
| | `203` | `+  assert stat.S_IMODE((tmp_path / "users" / "alice" / "integrations" / "lark-cli" / "config" / "locks").stat().st_mode) == 0o700  ` |
| `197` | `204` | `   assert stat.S_IMODE((tmp_path / "users" / "alice" / "integrations" / "lark-cli" / "data").stat().st_mode) == 0o700   ` |
| `198` | `205` | `   assert container_paths["/mnt/integrations/lark-cli/runtime"] == (   ` |
| `199` | `206` | `   str(runtime_dir),   ` |
| 

`   @@ -254,12 +261,14 @@ def test_get_extra_mounts_provisioner_payload_has_unique_container_paths(tmp_pat   `

 |
| `254` | `261` | `   assert "/mnt/skills/custom" in container_paths   ` |
| `255` | `262` | `   assert "/mnt/skills/integrations" in container_paths   ` |
| `256` | `263` | `   assert lark_cli.LARK_CLI_SANDBOX_CONFIG_DIR in container_paths   ` |
| | `264` | `+  assert lark_cli.LARK_CLI_SANDBOX_LOCKS_DIR in container_paths  ` |
| `257` | `265` | `   assert lark_cli.LARK_CLI_SANDBOX_DATA_DIR in container_paths   ` |
| `258` | `266` | `   assert lark_cli.LARK_CLI_SANDBOX_RUNTIME_DIR in container_paths   ` |
| `259` | `267` | `       ` |
| `260` | `268` | `   payload = remote_backend._provisioner_extra_mounts_payload(mounts)   ` |
| `261` | `269` | `   payload_paths = [str(item["container_path"]) for item in payload]   ` |
| `262` | `270` | `   assert len(payload_paths) == len(set(payload_paths))   ` |
| | `271` | `+  assert payload_paths.index(lark_cli.LARK_CLI_SANDBOX_CONFIG_DIR) < payload_paths.index(lark_cli.LARK_CLI_SANDBOX_LOCKS_DIR)  ` |
| `263` | `272` | `       ` |
| `264` | `273` | `   provisioner_module.DEER_FLOW_HOST_BASE_DIR = str(home)   ` |
| `265` | `274` | `   validated = provisioner_module._validated_extra_mounts([provisioner_module.ExtraMount(**item) for item in payload])   ` |
| 

`   @@ -271,6 +280,7 @@ def test_get_extra_mounts_provisioner_payload_has_unique_container_paths(tmp_pat   `

 |
| `271` | `280` | `   "/mnt/skills/custom",   ` |
| `272` | `281` | `   "/mnt/skills/integrations",   ` |
| `273` | `282` | `   lark_cli.LARK_CLI_SANDBOX_CONFIG_DIR,   ` |
| | `283` | `+  lark_cli.LARK_CLI_SANDBOX_LOCKS_DIR,  ` |
| `274` | `284` | `   lark_cli.LARK_CLI_SANDBOX_DATA_DIR,   ` |
| `275` | `285` | `   lark_cli.LARK_CLI_SANDBOX_RUNTIME_DIR,   ` |
| `276` | `286` | `   }   ` |
| 

 |

Collapse file

### [`‎backend/tests/test_lark_cli_integration.py‎`](https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0#diff-33b993b2c3c332fb8a828161d3090f34ce07402552cb791b179d7536cf5c8fc3)

Copy file name to clipboardExpand all lines: backend/tests/test\_lark\_cli\_integration.py

+1Lines changed: 1 addition & 0 deletions

| Original file line number | Diff line number | Diff line change |
| --- | --- | --- |
| 
`   @@ -1073,6 +1073,7 @@ def test_lark_cli_env_hardens_existing_credential_tree(monkeypatch, tmp_path) ->   `

 |
| `1073` | `1073` | `   lark_cli.lark_cli_env_overlay("alice")   ` |
| `1074` | `1074` | `       ` |
| `1075` | `1075` | `   assert stat.S_IMODE(config_dir.stat().st_mode) == 0o700   ` |
| | `1076` | `+  assert stat.S_IMODE((config_dir / "locks").stat().st_mode) == 0o700  ` |
| `1076` | `1077` | `   assert stat.S_IMODE(data_dir.stat().st_mode) == 0o700   ` |
| `1077` | `1078` | `   assert stat.S_IMODE(secret_file.stat().st_mode) == 0o600   ` |
| `1078` | `1079` | `   assert stat.S_IMODE(token_file.stat().st_mode) == 0o600   ` |
| 

 |

Collapse file

### [`‎backend/tests/test_provisioner_mount_contract.py‎`](https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0#diff-f3c542468dca7adcaa74b032c5fe70b6d53915ce921d376fa77a4f45f9201500)

Copy file name to clipboardExpand all lines: backend/tests/test\_provisioner\_mount\_contract.py

+2\-1Lines changed: 2 additions & 1 deletion

| Original file line number | Diff line number | Diff line change |
| --- | --- | --- |
| 
`   @@ -25,4 +25,5 @@ def test_gateway_and_provisioner_extra_mount_contracts_match() -> None:   `

 |
| `25` | `25` | `       ` |
| `26` | `26` | `   assert gateway_paths == provisioner_paths   ` |
| `27` | `27` | `   assert "/mnt/integrations/lark-cli/runtime" in gateway_paths   ` |
| `28` | | `-  assert _literal_assignment(provisioner_path, "MAX_EXTRA_MOUNTS") == 9  ` |
| | `28` | `+  assert "/mnt/integrations/lark-cli/config/locks" in gateway_paths  ` |
| | `29` | `+  assert _literal_assignment(provisioner_path, "MAX_EXTRA_MOUNTS") == 10  ` |

Collapse file

### [`‎backend/tests/test_provisioner_pvc_volumes.py‎`](https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0#diff-b535126ad90ad1116eba0ad347a72a028201a8e79e18ac86c0722f4e94a9b1ef)

Copy file name to clipboardExpand all lines: backend/tests/test\_provisioner\_pvc\_volumes.py

+27\-6Lines changed: 27 additions & 6 deletions

| Original file line number | Diff line number | Diff line change |
| --- | --- | --- |
| 
`   @@ -538,6 +538,11 @@ def test_runtime_extra_mount_dropped_when_init_container_enabled(self, provision   `

 |
| `538` | `538` | `   provisioner_module.ExtraMount(   ` |
| `539` | `539` | `   host_path="/state/users/alice/integrations/lark-cli/config",   ` |
| `540` | `540` | `   container_path="/mnt/integrations/lark-cli/config",   ` |
| | `541` | `+  read_only=True,  ` |
| | `542` | `+  ),  ` |
| | `543` | `+  provisioner_module.ExtraMount(  ` |
| | `544` | `+  host_path="/state/users/alice/integrations/lark-cli/config/locks",  ` |
| | `545` | `+  container_path="/mnt/integrations/lark-cli/config/locks",  ` |
| `541` | `546` | `   read_only=False,   ` |
| `542` | `547` | `   ),   ` |
| `543` | `548` | `   provisioner_module.ExtraMount(   ` |
| 

`   @@ -555,14 +560,18 @@ def test_runtime_extra_mount_dropped_when_init_container_enabled(self, provision   `

 |
| `555` | `560` | `   provision_lark_cli_runtime=True,   ` |
| `556` | `561` | `   )   ` |
| `557` | `562` | `       ` |
| `558` | | `-  # The credential config mount stays; the hostPath runtime extra mount is  ` |
| `559` | | `-  # replaced by the emptyDir supplied by the init container (so the runtime  ` |
| `560` | | `-  # path is not backed by an extra-* hostPath volume).  ` |
| | `563` | `+  # The read-only credential config and nested writable locks mounts stay;  ` |
| | `564` | `+  # the hostPath runtime extra mount is replaced by the emptyDir supplied  ` |
| | `565` | `+  # by the init container (so the runtime path is not backed by an extra-*  ` |
| | `566` | `+  # hostPath volume).  ` |
| `561` | `567` | `   runtime_mounts = [m for m in pod.spec.containers[0].volume_mounts if m.mount_path == "/mnt/integrations/lark-cli/runtime"]   ` |
| `562` | `568` | `   assert len(runtime_mounts) == 1   ` |
| `563` | `569` | `   assert runtime_mounts[0].name == provisioner_module.LARK_CLI_RUNTIME_VOLUME_NAME   ` |
| `564` | | `-  mount_paths = {m.mount_path for m in pod.spec.containers[0].volume_mounts}  ` |
| `565` | | `-  assert "/mnt/integrations/lark-cli/config" in mount_paths  ` |
| | `570` | `+  sandbox_mount_order = [m.mount_path for m in pod.spec.containers[0].volume_mounts]  ` |
| | `571` | `+  sandbox_mounts = {m.mount_path: m for m in pod.spec.containers[0].volume_mounts}  ` |
| | `572` | `+  assert sandbox_mounts["/mnt/integrations/lark-cli/config"].read_only is True  ` |
| | `573` | `+  assert sandbox_mounts["/mnt/integrations/lark-cli/config/locks"].read_only is False  ` |
| | `574` | `+  assert sandbox_mount_order.index("/mnt/integrations/lark-cli/config") < sandbox_mount_order.index("/mnt/integrations/lark-cli/config/locks")  ` |
| `566` | `575` | `       ` |
| `567` | `576` | `       ` |
| `568` | `577` | `   class TestLarkCliBrokerSidecar:   ` |
| 

`   @@ -576,6 +585,11 @@ def _credential_mounts(provisioner_module):   `

 |
| `576` | `585` | `   container_path="/mnt/integrations/lark-cli/config",   ` |
| `577` | `586` | `   read_only=True,   ` |
| `578` | `587` | `   ),   ` |
| | `588` | `+  provisioner_module.ExtraMount(  ` |
| | `589` | `+  host_path="/state/users/alice/integrations/lark-cli/config/locks",  ` |
| | `590` | `+  container_path="/mnt/integrations/lark-cli/config/locks",  ` |
| | `591` | `+  read_only=False,  ` |
| | `592` | `+  ),  ` |
| `579` | `593` | `   provisioner_module.ExtraMount(   ` |
| `580` | `594` | `   host_path="/state/users/alice/integrations/lark-cli/data",   ` |
| `581` | `595` | `   container_path="/mnt/integrations/lark-cli/data",   ` |
| 

`   @@ -635,15 +649,22 @@ def test_broker_sidecar_and_shim_when_enabled(self, provisioner_module):   `

 |
| `635` | `649` | `   assert sidecar.image == "deer-flow/lark-cli-broker:v1.0.65"   ` |
| `636` | `650` | `   assert sidecar.args == ["serve"]   ` |
| `637` | `651` | `   # Credentials mounted into the sidecar only.   ` |
| `638` | | `-  sidecar_paths = {m.mount_path for m in sidecar.volume_mounts}  ` |
| | `652` | `+  sidecar_mount_order = [m.mount_path for m in sidecar.volume_mounts]  ` |
| | `653` | `+  sidecar_mounts = {m.mount_path: m for m in sidecar.volume_mounts}  ` |
| | `654` | `+  sidecar_paths = set(sidecar_mounts)  ` |
| `639` | `655` | `   assert provisioner_module.LARK_BROKER_SIDECAR_CONFIG_PATH in sidecar_paths   ` |
| | `656` | `+  assert provisioner_module.LARK_BROKER_SIDECAR_LOCKS_PATH in sidecar_paths  ` |
| `640` | `657` | `   assert provisioner_module.LARK_BROKER_SIDECAR_DATA_PATH in sidecar_paths   ` |
| | `658` | `+  assert sidecar_mounts[provisioner_module.LARK_BROKER_SIDECAR_CONFIG_PATH].read_only is True  ` |
| | `659` | `+  assert sidecar_mounts[provisioner_module.LARK_BROKER_SIDECAR_LOCKS_PATH].read_only is False  ` |
| | `660` | `+  assert sidecar_mount_order.index(provisioner_module.LARK_BROKER_SIDECAR_CONFIG_PATH) < sidecar_mount_order.index(provisioner_module.LARK_BROKER_SIDECAR_LOCKS_PATH)  ` |
| `641` | `661` | `       ` |
| `642` | `662` | `   # Sandbox container: runtime shim mount + broker URL env, NO config/data.   ` |
| `643` | `663` | `   sandbox = pod.spec.containers[0]   ` |
| `644` | `664` | `   sandbox_paths = {m.mount_path for m in sandbox.volume_mounts}   ` |
| `645` | `665` | `   assert provisioner_module.LARK_CLI_RUNTIME_CONTAINER_PATH in sandbox_paths   ` |
| `646` | `666` | `   assert "/mnt/integrations/lark-cli/config" not in sandbox_paths   ` |
| | `667` | `+  assert "/mnt/integrations/lark-cli/config/locks" not in sandbox_paths  ` |
| `647` | `668` | `   assert "/mnt/integrations/lark-cli/data" not in sandbox_paths   ` |
| `648` | `669` | `   env = {e.name: e.value for e in (sandbox.env or [])}   ` |
| `649` | `670` | `   assert env.get("DEERFLOW_LARK_BROKER_URL") == provisioner_module.LARK_BROKER_URL   ` |
| 

 |

Collapse file

### [`‎docker/provisioner/app.py‎`](https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0#diff-58d8fa3c8a63efdb78f966f7760a492f315153a3cb1c2838870377ee550fe0cd)

Copy file name to clipboardExpand all lines: docker/provisioner/app.py

+29\-15Lines changed: 29 additions & 15 deletions

| Original file line number | Diff line number | Diff line change |
| --- | --- | --- |
| 
`   @@ -70,19 +70,23 @@   `

 |
| `70` | `70` | `   # Optional "lark-cli broker" image (Pattern B, issue #4338). When set, sandbox   ` |
| `71` | `71` | `   # Pods requesting the broker get an init container that stages a shim + a   ` |
| `72` | `72` | `   # long-running broker sidecar that holds the credentials, instead of mounting the   ` |
| `73` | | `-  # plaintext config/data credential dirs into the sandbox container. Empty ⇒ broker  ` |
| `74` | | `-  # off (Pattern A / legacy behavior). Broker supersedes Pattern A when both are set.  ` |
| | `73` | `+  # plaintext config/locks/data credential dirs into the sandbox container. Empty ⇒  ` |
| | `74` | `+  # broker off (Pattern A / legacy behavior). Broker supersedes Pattern A when both  ` |
| | `75` | `+  # are set.  ` |
| `75` | `76` | `   LARK_CLI_BROKER_IMAGE = os.environ.get("LARK_CLI_BROKER_IMAGE", "")   ` |
| `76` | `77` | `   # Optional comma-separated lark-cli subcommand denylist forwarded to the broker   ` |
| `77` | `78` | `   # sidecar (issue #4338 hardening). Empty ⇒ no subcommand is blocked. See the   ` |
| `78` | `79` | `   # broker README's "subcommand denylist" section.   ` |
| `79` | `80` | `   LARK_CLI_BROKER_DENY_SUBCOMMANDS = os.environ.get("DEERFLOW_LARK_BROKER_DENY_SUBCOMMANDS", "")   ` |
| `80` | `81` | `   LARK_CLI_CONFIG_CONTAINER_PATH = "/mnt/integrations/lark-cli/config"   ` |
| | `82` | `+  LARK_CLI_LOCKS_CONTAINER_PATH = f"{LARK_CLI_CONFIG_CONTAINER_PATH}/locks"  ` |
| `81` | `83` | `   LARK_CLI_DATA_CONTAINER_PATH = "/mnt/integrations/lark-cli/data"   ` |
| `82` | `84` | `   # Where the broker sidecar reads the per-user credentials (sidecar-only paths).   ` |
| `83` | `85` | `   LARK_BROKER_SIDECAR_CONFIG_PATH = "/var/lark/config"   ` |
| | `86` | `+  LARK_BROKER_SIDECAR_LOCKS_PATH = f"{LARK_BROKER_SIDECAR_CONFIG_PATH}/locks"  ` |
| `84` | `87` | `   LARK_BROKER_SIDECAR_DATA_PATH = "/var/lark/data"   ` |
| `85` | `88` | `   LARK_BROKER_CONFIG_VOLUME_NAME = "lark-cli-config"   ` |
| | `89` | `+  LARK_BROKER_LOCKS_VOLUME_NAME = "lark-cli-locks"  ` |
| `86` | `90` | `   LARK_BROKER_DATA_VOLUME_NAME = "lark-cli-data"   ` |
| `87` | `91` | `   LARK_BROKER_URL = "http://127.0.0.1:8788"   ` |
| `88` | `92` | `   THREADS_HOST_PATH = os.environ.get("THREADS_HOST_PATH", "/.deer-flow/threads")   ` |
| 

`   @@ -103,12 +107,13 @@   `

 |
| `103` | `107` | `   SAFE_THREAD_ID_PATTERN = r"^[A-Za-z0-9_-]{1,64}$"   ` |
| `104` | `108` | `   SAFE_USER_ID_PATTERN = r"^[A-Za-z0-9_\-]+$"   ` |
| `105` | `109` | `   DEFAULT_USER_ID = "default"   ` |
| `106` | | `-  MAX_EXTRA_MOUNTS = 9  ` |
| | `110` | `+  MAX_EXTRA_MOUNTS = 10  ` |
| `107` | `111` | `   ALLOWED_EXTRA_MOUNT_PATHS = {   ` |
| `108` | `112` | `   "/mnt/acp-workspace",   ` |
| `109` | `113` | `   "/mnt/skills/custom",   ` |
| `110` | `114` | `   "/mnt/skills/integrations",   ` |
| `111` | `115` | `   "/mnt/integrations/lark-cli/config",   ` |
| | `116` | `+  "/mnt/integrations/lark-cli/config/locks",  ` |
| `112` | `117` | `   "/mnt/integrations/lark-cli/data",   ` |
| `113` | `118` | `   "/mnt/integrations/lark-cli/runtime",   ` |
| `114` | `119` | `   }   ` |
| 

`   @@ -231,18 +236,20 @@ def _runtime_provided_extra_mounts(   `

 |
| `231` | `236` | |
| `232` | `237` | `   Pattern A (init container + emptyDir) provides   ` |
| `233` | `238` | `   ``/mnt/integrations/lark-cli/runtime``, so a hostPath/PVC mount at the same   ` |
| `234` | | `-  path would collide — it is dropped, leaving the per-user ``config`` / ``data``  ` |
| `235` | | `-  credential mounts intact.  ` |
| | `239` | `+  path would collide — it is dropped, leaving the per-user ``config`` /  ` |
| | `240` | `+  ``config/locks`` / ``data`` mounts intact. The nested locks mount is writable  ` |
| | `241` | `+  so lark-cli can coordinate API calls while the config root remains read-only.  ` |
| `236` | `242` | |
| `237` | | `-  Pattern B (broker sidecar) additionally moves the ``config`` / ``data``  ` |
| `238` | | `-  credential mounts off the *sandbox* container and into the sidecar, so those  ` |
| `239` | | `-  are dropped here too — the sandbox never sees plaintext credentials.  ` |
| | `243` | `+  Pattern B (broker sidecar) additionally moves all three mounts off the  ` |
| | `244` | `+  *sandbox* container and into the sidecar, so those are dropped here too —  ` |
| | `245` | `+  the sandbox never sees plaintext credentials.  ` |
| `240` | `246` | `   """   ` |
| `241` | `247` | `   dropped: set[str] = set()   ` |
| `242` | `248` | `   if _lark_cli_broker_enabled(provision_lark_cli_broker):   ` |
| `243` | `249` | `   dropped = {   ` |
| `244` | `250` | `   LARK_CLI_RUNTIME_CONTAINER_PATH,   ` |
| `245` | `251` | `   LARK_CLI_CONFIG_CONTAINER_PATH,   ` |
| | `252` | `+  LARK_CLI_LOCKS_CONTAINER_PATH,  ` |
| `246` | `253` | `   LARK_CLI_DATA_CONTAINER_PATH,   ` |
| `247` | `254` | `   }   ` |
| `248` | `255` | `   elif _lark_cli_runtime_enabled(provision_lark_cli_runtime):   ` |
| 

`   @@ -253,15 +260,19 @@ def _runtime_provided_extra_mounts(   `

 |
| `253` | `260` | `       ` |
| `254` | `261` | `       ` |
| `255` | `262` | `   def _lark_broker_credential_mounts(extra_mounts: list["ExtraMount"] | None) -> dict[str, "ExtraMount"]:   ` |
| `256` | | `-  """Extract the config/data credential mounts the broker sidecar needs.  ` |
| | `263` | `+  """Extract the config/locks/data mounts the broker sidecar needs.  ` |
| `257` | `264` | |
| `258` | `265` | `   Keyed by container path so the caller can wire each into the sidecar's fixed   ` |
| `259` | | `-  ``/var/lark/{config,data}`` paths.  ` |
| | `266` | `+  ``/var/lark/{config,config/locks,data}`` paths.  ` |
| `260` | `267` | `   """   ` |
| `261` | `268` | `   result: dict[str, ExtraMount] = {}   ` |
| `262` | `269` | `   for mount in _validated_extra_mounts(extra_mounts):   ` |
| `263` | `270` | `   normalized = posixpath.normpath(mount.container_path)   ` |
| `264` | | `-  if normalized in (LARK_CLI_CONFIG_CONTAINER_PATH, LARK_CLI_DATA_CONTAINER_PATH):  ` |
| | `271` | `+  if normalized in (  ` |
| | `272` | `+  LARK_CLI_CONFIG_CONTAINER_PATH,  ` |
| | `273` | `+  LARK_CLI_LOCKS_CONTAINER_PATH,  ` |
| | `274` | `+  LARK_CLI_DATA_CONTAINER_PATH,  ` |
| | `275` | `+  ):  ` |
| `265` | `276` | `   result[normalized] = mount   ` |
| `266` | `277` | `   return result   ` |
| `267` | `278` | `       ` |
| 

`   @@ -590,11 +601,12 @@ def _build_volumes(   `

 |
| `590` | `601` | `   empty_dir=k8s_client.V1EmptyDirVolumeSource(),   ` |
| `591` | `602` | `   )   ` |
| `592` | `603` | `   )   ` |
| `593` | | `-  # Pattern B: the config/data credential volumes go to the broker sidecar only.  ` |
| | `604` | `+  # Pattern B: config/locks/data volumes go to the broker sidecar only.  ` |
| `594` | `605` | `   if _lark_cli_broker_enabled(provision_lark_cli_broker):   ` |
| `595` | `606` | `   credential_mounts = _lark_broker_credential_mounts(extra_mounts)   ` |
| `596` | `607` | `   for container_path, volume_name in (   ` |
| `597` | `608` | `   (LARK_CLI_CONFIG_CONTAINER_PATH, LARK_BROKER_CONFIG_VOLUME_NAME),   ` |
| | `609` | `+  (LARK_CLI_LOCKS_CONTAINER_PATH, LARK_BROKER_LOCKS_VOLUME_NAME),  ` |
| `598` | `610` | `   (LARK_CLI_DATA_CONTAINER_PATH, LARK_BROKER_DATA_VOLUME_NAME),   ` |
| `599` | `611` | `   ):   ` |
| `600` | `612` | `   mount = credential_mounts.get(container_path)   ` |
| 

`   @@ -757,16 +769,18 @@ def _build_lark_cli_broker_sidecars(   `

 |
| `757` | `769` | `   ) -> list[k8s_client.V1Container]:   ` |
| `758` | `770` | `   """Broker sidecar that holds lark-cli + the per-user credentials (Pattern B).   ` |
| `759` | `771` | |
| `760` | | `-  The config/data credential dirs are mounted **only** here (never on the  ` |
| `761` | | `-  sandbox container), so the plaintext app secret / OAuth tokens stay out of  ` |
| `762` | | `-  the sandbox filesystem. The broker serves the command surface on loopback.  ` |
| | `772` | `+  The config/locks/data dirs are mounted **only** here (never on the sandbox  ` |
| | `773` | `+  container), so the plaintext app secret / OAuth tokens stay out of the  ` |
| | `774` | `+  sandbox filesystem. The config root is read-only and its nested locks mount  ` |
| | `775` | `+  is writable. The broker serves the command surface on loopback.  ` |
| `763` | `776` | `   """   ` |
| `764` | `777` | `   if not _lark_cli_broker_enabled(provision_lark_cli_broker):   ` |
| `765` | `778` | `   return []   ` |
| `766` | `779` | `   credential_mounts = _lark_broker_credential_mounts(extra_mounts)   ` |
| `767` | `780` | `   volume_mounts: list[k8s_client.V1VolumeMount] = []   ` |
| `768` | `781` | `   for container_path, volume_name, sidecar_path in (   ` |
| `769` | `782` | `   (LARK_CLI_CONFIG_CONTAINER_PATH, LARK_BROKER_CONFIG_VOLUME_NAME, LARK_BROKER_SIDECAR_CONFIG_PATH),   ` |
| | `783` | `+  (LARK_CLI_LOCKS_CONTAINER_PATH, LARK_BROKER_LOCKS_VOLUME_NAME, LARK_BROKER_SIDECAR_LOCKS_PATH),  ` |
| `770` | `784` | `   (LARK_CLI_DATA_CONTAINER_PATH, LARK_BROKER_DATA_VOLUME_NAME, LARK_BROKER_SIDECAR_DATA_PATH),   ` |
| `771` | `785` | `   ):   ` |
| `772` | `786` | `   mount = credential_mounts.get(container_path)   ` |
| 

 |

## 0 commit comments

Comments

0 (0)

Please [sign in](https://github.com/login?return_to=https://github.com/bytedance/deer-flow/commit/17531d7c118d6111b863f945ff910a7889a235b0) to comment.